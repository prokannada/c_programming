#include <stdio.h>

int main() {
    int number; // number is a variable of type integer.

    number = 1;
    printf("number value is = %d\n", number);

    number = 2;
    printf("number value is = %d\n", number);
    return 0;
}