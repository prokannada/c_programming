#include <stdio.h>
#include <stdbool.h>

int main() {
    int age;
    float percentage;
    char gender;
    bool isEligible;

    age = 20;
    percentage = 90.67;
    gender = 'm';
    isEligible = false;

    printf("value of age is = %d\n", age);
    printf("value of percentage is = %f\n", percentage);
    printf("value of gender is = %c\n", gender);
    printf("value of isEligible is = %d\n", isEligible);

    return 0;
}