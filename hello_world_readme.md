# C Program which prints "Hello World!" to standard output.

## Command for compilation using gcc from terminal.

```console
gcc hello_world.c
```

## How to run the a.out executable in the terminal.

```console
./a.out
```
